## Showcase entry

<!--
Add your entry to src/data/showcase.json following this shape:
{
  "id": "my-site",
  "title": "My Site",
  "description": "One or two sentences.",
  "components": ["ticket-card", "barcode"],
  "author": "Team name"
}
Slugs under "components" must each resolve to /docs/[slug].
-->

- [ ] Entry added to `src/data/showcase.json`
- [ ] All `components` slugs resolve to existing docs pages
- [ ] `npm test` passes (dead-preview detector covers showcase links)
- [ ] No external image hotlinks without consent (prefer repo-hosted `public/showcase/`)
