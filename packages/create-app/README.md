# create-bigbull-app

Scaffold a Next.js + Tailwind CSS v4 + bigbullui starter in one command.

```bash
npx create-bigbull-app my-app
cd my-app
npm run dev
```

Skip dependency installation with `--no-install`. MIT licensed.
